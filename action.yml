name: setup-stacc-cli
description: Install the stacc CLI on Github Actions runners
inputs:
  version:
    description: "Version of stacc CLI"
    required: false
    default: 0.7.57
runs:
  using: "node16"
  main: "src/index.js"
